# Developer guide

## Release Process

The current release process is mostly manual, in your PR:
- Create a new version document under `doc/changes/`.
- Add a reference to the new version document in `changelog.md`.
- Merge in  your PR with changes.
- Add a version tag: This will prompt a proper release via workflow.